query Property($id: Int!, ) {
  property(id: $id) {
      id
      title
      propertyType
      image
      description
      pricePerNight
      address
      amenities
      createdAt
      updatedAt
      owner {
        id
        email
        image
        fullName
      }
  }
} 