query Property($id: Int!, ) {
  property(id: $id) {
      id
      title
      propertyType
      image
      description
      pricePerNight
      address
      beds
      bedrooms
      amenities
      createdAt
      updatedAt
      creator {
        id
        email
        image
        fullName
      }
  }
} 