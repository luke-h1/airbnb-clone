mutation CreateProperty($options: CreatePropertyInput!) { 
  createProperty(options: $options) {
    userId
    title
    propertyType
    mainImage
    latitude
    longitude
    amenities
    pricePerNight
    createdAt
    updatedAt
  }
}
