query Property($id: Int!) {
  property(id: $id) {
      id
      title
      creatorId
      propertyType
      mainImage
      description
      pricePerNight
      latitude
      longitude
      amenities
      createdAt
      updatedAt
      creator {
        id
        email
        picture
        fullName
      }
  }
} 